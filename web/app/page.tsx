export default function Home() {
  return (
    <main>
      <section className="panel">
        <p className="eyebrow">Local backend</p>
        <h1>AI Browser Agent</h1>
        <p>
          The dashboard and API are ready. Build the Chrome extension, load the
          <code> extension </code>
          folder in Chrome, then click the floating AI Assist button on a job form or email request page.
        </p>
      </section>
    </main>
  );
}
