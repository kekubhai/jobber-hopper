{
  "name": "@jobber-hopper/extension",
  "private": true,
  "scripts": {
    "build": "node scripts/sync-profile-match.cjs && tsc -p tsconfig.json && node scripts/verify-build.cjs",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@jobber-hopper/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
